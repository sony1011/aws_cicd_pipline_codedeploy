version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install --force
  build:
    commands:
      # npm run build
      - npm run-script build
artifacts:
  files:
    - scripts/**/*

