version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
  commands:
      - node -v
      - npm -v
    commands:
      - npm install --force
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'

